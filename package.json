{
  "name": "opencode-lifeline-plugin",
  "version": "0.1.0",
  "description": "OpenCode plugin that lets a smaller model phone-a-friend (stronger advisor model) when stuck",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.1.0",
    "@opencode-ai/sdk": "^1.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^5.0.0"
  },
  "license": "MIT"
}
